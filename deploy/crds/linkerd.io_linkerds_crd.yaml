apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: linkerds.linkerd.io
spec:
  group: linkerd.io
  names:
    kind: Linkerd
    listKind: LinkerdList
    plural: linkerds
    singular: linkerd
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Linkerd is the Schema for the linkerds API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: LinkerdSpec defines the desired state of Linkerd
            properties:
              logLevel:
                type: string
              size:
                description: Size is the size of the linkerd deployment
                format: int32
                type: integer
            required:
            - logLevel
            - size
            type: object
          status:
            description: LinkerdStatus defines the observed state of Linkerd
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
